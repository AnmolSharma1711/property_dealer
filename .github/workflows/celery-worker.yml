name: ❌ DEPRECATED - Celery Worker (No Longer Used)

# ⚠️ DEPRECATED - Celery has been removed from the application
# 
# AI analysis now runs synchronously in background threads
# instead of using Celery task queues. This workflow is no longer needed.
#
# To delete: rm .github/workflows/celery-worker.yml

on:
  workflow_dispatch:  # Manual trigger only - disabled

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: ❌ This workflow is deprecated
        run: |
          echo "This workflow has been deprecated."
          echo "Celery and Redis have been removed from the application."
          echo "AI analysis now runs synchronously in background threads."
          exit 1
